using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Diagnostics;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace QuickBite.Platforms.iOS
{
    public partial class Entitlements: Component
    {    
        public Entitlements()
        {
            InitializeComponent();
        }

        public Entitlements(IContainer container)
        {
            container.Add(this);

            InitializeComponent();
        }
    }
}
